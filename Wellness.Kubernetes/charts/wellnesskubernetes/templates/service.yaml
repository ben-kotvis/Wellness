apiVersion: v1
kind: Service
metadata:
  name: {{ template "wellnesskubernetes.fullname" . }}
  labels:
    app: {{ template "wellnesskubernetes.name" . }}
    chart: {{ template "wellnesskubernetes.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "wellnesskubernetes.name" . }}
    release: {{ .Release.Name }}
